#!/bin/sh
set -e

#!/bin/sh
set -e

source ./globals

echo "Compressing Busybox source code in '$BUSYBOX_SOURCES_DIR' into zip file"

cd "$BUSYBOX_SOURCES_DIR"
zip -r busybox.zip .

mv "$BUSYBOX_SOURCES_DIR/busybox.zip" "$BUSYBOX_COMPILED_DIR/"
